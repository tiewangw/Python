# 创建一个向量（NumPy）

import numpy as np
# 创建一个行向量
vector_row = np.array([1,2,3])
# 创建一个列向量
vector_rcolumn = np.array([ [1],
                            [2],
                            [3]])

print(vector_row)
print(vector_rcolumn)